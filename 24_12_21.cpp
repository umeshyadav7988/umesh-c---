#include<iostream>
using namespace std;

int main(){
    char a[]="python";
    cout<<a<<endl;
    for(int i=0;a[i]!='\0';i++){
    cout<<a[i]<<endl;}
    return 0;

    // char a[]="c\0++";
    // cout<<a<<endl;

    // char a[50];
    // cin.getline(a,50);
    // cout<<a<<endl;
    // int i,count=0;
    // for(i=0;a[i]!='\0';i++){
        // if(a[i]==' ')
        // count++;
    // }
    // cout<<count<<endl;

    // char a[50];
    // cin.getline(a,50);
    // cout<<a<<endl;
    // int i,count=0;
    // for(i=0;a[i]!='\0';i++){
        // if(a[i]>='a' && a[i]<='z')
        // count++;
        // }
        // cout<<count<<endl;


        // char a[50];
        // cin.getline(a,50);
        // cout<<a<<endl;
        // int i,count=0;
        // for(i=0;a[i]!='\0';i++){
            // if(a[i]>='a' && a[i]<='z')
            // a[i]=a[i]-32;
            // cout<<a[i]<<endl;
            // }
            // cout<<a<<endl;

            // char a[50];
            // cin.getline(a,50);
            // cout<<a<<endl;
            // int i,count=0;
            // for(i=0;a[i]!='\0';i++){
                // if(a[i]>='a' && a[i]<='z')
                // a[i]=a[i]-32;
            // else if(a[i]>='A' && a[i]<='z')
            // a[i]=a[i]+32;
            // }
                // cout<<a<<endl;


                // int i;
                // char a[27];
                // char input=65;
                // for(i=0;i<26;i++){
                // a[i]=input;
                // input++;
                // }
                // a[i]='\0';
                // cout<<a;

}